apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: keycloak
spec:
  chart:
    spec:
      chart: keycloakx
      sourceRef:
        kind: HelmRepository
        name: keycloak
      version: "7.2.0"
  interval: 1h0m0s
  valuesFrom:
    - kind: ConfigMap
      name: keycloak-values
